[project]
name = "llm-synchronizer"
version = "0.1.0"
description = "Script to synchronise AI agent and command configurations across multiple providers"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0.3"]
