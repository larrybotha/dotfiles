#!/usr/bin/env bash

function installmodule() {
	if npm list -g "${@}" | grep "empty" > /dev/null; then
		npm install -g "${@}"
	else
		echo "${@} is already installed."
	fi
}

installmodule bower
installmodule coffee-script
installmodule denode
installmodule devtool
installmodule elm
installmodule elm-format@exp
installmodule elm-oracle
installmodule elm-test
installmodule feathers-cli
installmodule gulp-cli
installmodule server
installmodule imageoptim-cli
installmodule mocha-phantomjs
installmodule ngrok
installmodule prettier
installmodule react-native-cli
installmodule svgo
installmodule testem
installmodule yo
