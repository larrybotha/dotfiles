#!/usr/bin/env bash

if ! type npm
	echo 'npm is not a recognised command'

npm install -g bower
npm install -g gulp
npm install -g jquery
npm install -g jsdom
npm install -g jasmine-node
npm install -g jasmine-jquery
npm install -g yo
